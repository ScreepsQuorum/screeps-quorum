module.exports = {
  // Match StandardJS
  semi: false,
  // Match StandardJS
  singleQuote: true,
  // Greatly improved git diffs. Adding keys to objects doesn't cause a change
  // on the line before just for a comma.
  trailingComma: 'es5',
}
